import { MonthlyScheduleRequest } from './monthly-schedule-request';

describe('MonthlyScheduleRequest', () => {
  it('should create an instance', () => {
    expect(new MonthlyScheduleRequest()).toBeTruthy();
  });
});
